fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
aiosqlite>=0.19
alembic>=1.13
camoufox==0.4.11
# 必须 pin 1.60.0:playwright 1.61 给 Browser.setDefaultViewport 的 viewport 加了 isMobile 字段,
# camoufox 0.4.11 打包的 Juggler schema 只认 viewportSize/deviceScaleFactor → 启浏览器协议报错
# (camoufox#653)。camoufox 已是最新升无可升,故锁 playwright 到兼容版。别放开上界。
playwright==1.60.0
httpx>=0.27
Pillow>=10.0
cryptography>=42.0
pydantic-settings>=2.1
loguru>=0.7
pytest>=8.0
pytest-asyncio>=0.23
openai>=1.0
# 视频管线 dub 阶段拼轨（sample 级 overlay/mix wav），M3a transport 链引入；配 ffmpeg 使用。
# 其余视频运行时依赖（dashscope SDK / yt-dlp / demucs 可选）由 M4 requirements 统一收口。
pydub>=0.25
openpyxl>=3.1
fastmcp>=3.4,<4

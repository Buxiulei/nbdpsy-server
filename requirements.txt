fastapi>=0.110
uvicorn[standard]>=0.27
sqlalchemy>=2.0
aiosqlite>=0.19
alembic>=1.13
camoufox==0.4.11
# 必须 pin 1.60.0:playwright 1.61 给 Browser.setDefaultViewport 的 viewport 加了 isMobile 字段,
# camoufox 0.4.11 打包的 Juggler schema 只认 viewportSize/deviceScaleFactor → 启浏览器协议报错
# (camoufox#653)。camoufox 已是最新升无可升,故锁 playwright 到兼容版。别放开上界。
playwright==1.60.0
httpx>=0.27
Pillow>=10.0
cryptography>=42.0
pydantic-settings>=2.1
loguru>=0.7
pytest>=8.0
pytest-asyncio>=0.23
openai>=1.0
# 视频管线运行时依赖：
# - pydub：dub 阶段拼轨（sample 级 overlay/mix wav），配 ffmpeg 使用（M3a 引入）。
# - dashscope：ASR 录音文件识别 SDK（providers 惰性 import；缺则 ASR 阶段 ImportError）。
# - yt-dlp：download 阶段 `python -m yt_dlp` 拉源视频 + 字幕（downloader）。
# - numpy：remake 像素向量化（analyzer 抽帧特征 / programmatic 生球 / tones 双侧音轨）。
# demucs（BGM 分离）仍为可选，缺失时 muxer 优雅降级为纯配音替换，故不入 requirements。
pydub>=0.25
dashscope>=1.20
yt-dlp>=2024.1
numpy>=1.26
openpyxl>=3.1
fastmcp>=3.4,<4
